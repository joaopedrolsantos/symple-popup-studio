Primary call to action — the only button treatment the brand material shows.

```jsx
<GradientButton icon="download">Download logos</GradientButton>
```

Gradient is `linear-gradient(110.521deg, #D1671B 14.07%, #422109 89.20%)` with `0 24px 32px -24px rgba(8,23,14,.88), 0 0 32px rgba(244,121,32,.16)`. Label is Work Sans Bold, -4% tracking, underlined, with a soft white text glow. `size="md"|"sm"` for in-page UI; keep `lg` for hero and deck use.
